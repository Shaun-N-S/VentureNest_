services:
  backend:
    build:
      context: ./Backend
    container_name: backend
    ports:
      - "4000:4000"
    env_file:
      - ./Backend/.env

  frontend:
    build:
      context: ./Frontend
      args:
        VITE_API_BASE_URL: /api
    container_name: frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
