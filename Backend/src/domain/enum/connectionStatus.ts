export enum RelationshipType {
  FOLLOW = "follow",
  CONNECTION = "connection",
}

export enum ConnectionStatus {
  NONE = "none",
  PENDING = "pending",
  ACCEPTED = "accepted",
  REJECTED = "rejected",
  CANCELLED = "cancelled",
}
